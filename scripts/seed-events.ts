/**
 * TODO: Seed script for demo events — run with `npx tsx scripts/seed-events.ts`
 * Requires: approved organizer + admin user in database
 */
console.log("Seed script placeholder — implement in next phase");
