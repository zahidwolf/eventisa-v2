import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function Page() {
  return (
    <AdminPlaceholder
      title="Security center"
      description="Login logs, failed attempts, sessions — 2FA placeholder."
    />
  );
}
