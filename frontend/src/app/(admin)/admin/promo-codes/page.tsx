import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function AdminPromoCodesPage() {
  return <AdminPlaceholder title="Promo codes" description="Global promo code oversight (per-event codes in organizer tools)." />;
}
