NEXT_PUBLIC_APP_NAME=Eventisa
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:5001/api
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_DEFAULT_CURRENCY=BDT
NEXT_PUBLIC_TIMEZONE=Asia/Dhaka

# Payment UI mirrors backend feature flags (fetched from GET /api/payments/config)
# PAYMENT_MODE=mock on backend — only Demo Payment is active locally
